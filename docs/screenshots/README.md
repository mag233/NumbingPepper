# Screenshots directory

Drop your actual images here (for example `reader.png`, `writer.png`, `highlights-chat.png`) so the README placeholders resolve.
Keep the files under `docs/screenshots/` and reference them via the relative path shown in `README.md`.
